{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "title": "Name",
      "description": "Friendly display name for this agent."
    },
    "description": {
      "type": "string",
      "title": "Description",
      "description": "Short description of what this agent specializes in. Shown to other agents for routing decisions.",
      "x-widget": "textarea"
    },
    "model": {
      "type": "string",
      "title": "Model",
      "description": "Qualified model override (e.g. openai:gpt-5.1). Defaults to global model.",
      "x-placeholder": "Default",
      "x-suggest": "model"
    },
    "systemPrompt": {
      "type": "string",
      "title": "System Prompt",
      "description": "Per-agent system prompt override.",
      "x-widget": "textarea"
    },
    "canMessage": {
      "type": "array",
      "title": "Can Message",
      "description": "Agent IDs this agent can talk to. Use * for all.",
      "items": { "type": "string" }
    },
    "tools": {
      "type": "array",
      "title": "Tools",
      "description": "Tool names to allow. Leave empty to allow all tools.",
      "items": { "type": "string" },
      "x-suggest": "tool"
    },
    "skills": {
      "type": "array",
      "title": "Skills",
      "description": "Skill names to allow. Leave empty to allow all skills.",
      "items": { "type": "string" },
      "x-suggest": "skill"
    }
  },
  "x-sections": [
    { "id": "general", "title": "General", "description": "Core agent settings. Model-level limits are configured under global models.", "properties": ["name", "description", "model", "systemPrompt", "canMessage", "tools", "skills"] }
  ]
}
