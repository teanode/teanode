// Package gateway provides the HTTP and WebSocket server that connects
// web clients, terminal relays, and browser extensions to the agent.
package gateway

import "github.com/teanode/teanode/internal/logging"

var log = logging.Get("gateway")
